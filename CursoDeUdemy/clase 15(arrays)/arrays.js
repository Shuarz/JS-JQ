'use strict';

//arrays

var paises = ['españa', 'alemania', 'korea', 'brasil'];

for (var index = 0; index < paises.length; index ++) //length me da el largo de la variable paises, que ahi es donde frena el for
    console.log(paises[index]); //aca lo muestra en listado el console en una sola linea
    
